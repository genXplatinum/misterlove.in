/* ============================================================
   PUNJAB: THE WHOLE TRUTH

   No web edition yet. The four written parts exist only as
   rendered PDFs — no HTML source survives — and recovering the
   prose from their typography is not finished, so the site
   publishes the collected PDF and announces the series rather
   than shipping a reader built on text that is not yet verified
   word-for-word against the book.

   scripts/import-punjab.mjs is that recovery, in progress.
   When it passes its guardrails and a word-diff against the
   source, it writes this file and the manifest's `live` moves
   off zero.
   ============================================================ */

const parts = [];

export default parts;
